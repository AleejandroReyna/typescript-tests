const first_name:string = "Alejandro"
const last_name:string = "Reyna"
const age:number = 28
const show:boolean = true

if(show) {
  console.log(`I'm ${first_name} ${last_name} and i'm ${age} years old.`)
}